{
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"TOOL REMINDERS (mandatory, every message):\\n- Code search: use semble MCP tools first. Use repo=https://github.com/acredsfan/lawnberry_pi unless local-path indexing is explicitly verified healthy in this session.\\n- Memory: use available ForgeMind memory/session tools in this runtime; do not hardcode unavailable method names.\"}}'"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"MANDATORY TOOL RULES - follow these every session without being reminded:\\n1. Code search: use semble MCP tools before grep/find for code lookup. Prefer repo=https://github.com/acredsfan/lawnberry_pi unless local indexing is confirmed healthy.\\n2. Memory: use available ForgeMind memory/session tools for memory operations; avoid pinning to tool names that are not present in the current runtime.\"}}'"
          }
        ]
      }
    ]
  }
}
