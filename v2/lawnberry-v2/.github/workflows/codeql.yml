name: codeql
on:
  push: { branches: [ "v2-spec-rebuild", "main" ] }
  pull_request:
  schedule: [ { cron: "0 3 * * 1" } ]
jobs:
  analyze:
    permissions: { actions: read, contents: read, security-events: write }
    uses: github/codeql-action/.github/workflows/codeql.yml@v3
    with: { languages: "python,javascript" }
